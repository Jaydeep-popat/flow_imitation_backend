# config

This folder is for application configuration (env parsing, constants, per-environment config).

Suggested files (as the project grows):
- `env.ts` (parse/validate process.env)
- `constants.ts`
- `cors.ts`
